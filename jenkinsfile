node{
    def app
    
    stage('Clone'){
        checkout scm
    }
    
    stage('Run image') {
        docker.image('xavki/nginx').withRun(' -p 80:80') { c ->
        sh 'docker ps'
        sh 'petit teste de jospin'
        sh 'curl loclhost'
        
    }
}
